declare const SUPPORTED_EXTENSIONS: {
    readonly image: Set<string>;
    readonly audio: Set<string>;
    readonly video: Set<string>;
    readonly pdf: Set<string>;
};
type FileType = keyof typeof SUPPORTED_EXTENSIONS;
/**
 * Processes a file path or URL and returns the file buffer and metadata
 * @param filePath Path to local file or URL
 * @param type Type of file to process
 * @returns Promise with buffer and metadata
 */
export declare const processFile: (filePath: string, type: FileType) => Promise<{
    buffer: Buffer;
    filename: string;
    filepath: string;
}>;
/**
 * @deprecated Use processFile instead
 */
export declare const attachFile: (filePath: string) => Promise<string>;
export {};
